# Model calls (scoring + question extraction), through OpenRouter
openrouter>=1.1.74

# Local embeddings. Pulls in torch + transformers; ~2.5GB installed.
sentence-transformers>=3.0
numpy>=1.26

# arXiv API + config
requests>=2.31
PyYAML>=6.0
